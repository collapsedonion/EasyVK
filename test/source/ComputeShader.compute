#version 450

layout(binding=0) buffer TestIn{
    int inputData[];
};

layout(binding=1) buffer TestOut{
    int outputData[];
};

layout(local_size_x = 6) in;

void main(){
    outputData[gl_GlobalInvocationID.x] += inputData[gl_GlobalInvocationID.x];
}